var assert = require('assert')
var Cachedb = require('../db/cachedb')

describe('Cachedb for blacklist token', function() {
	describe('#blacklistToken.init()', function() {
		it('should init model', function() {
			assert.equal([1,2,3].indexOf(4), -1)
		})
	})
})
